# GPStitch Fork Slot

This directory is reserved for the GPL-compatible vendored fork of
`Romancha/GPStitch`.

Do not fake this dependency. Before wiring commands to it, import or add the
fork as an explicit submodule/source subtree, keep GPL-3.0 notices, and record
which GPStitch commands RoadWatcher calls for telemetry sync and overlay work.
