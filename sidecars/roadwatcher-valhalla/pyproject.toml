[project]
name = "roadwatcher-valhalla-environment"
version = "0.1.0"
description = "Locked Windows x64 pyvalhalla runtime definition for RoadWatcher"
requires-python = "==3.12.*"
dependencies = [
  "pyvalhalla==3.7.0",
]

[tool.uv]
package = false
